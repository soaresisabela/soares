function setup() {
  createCanvas(800, 800);
  background("white");
}

function draw() {
  stroke("blue");
  fill("purple");

  // console.log (mouseIsPressed)

  if (mouseIsPressed) {
    rect(mouseX, mouseY, 100, 100);
  }
}
